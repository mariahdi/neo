# Skill: usafa-web (EXAMPLE STUB)

> Copy this to `usafa-web.md` and fill in the real site details. The real
> file is gitignored; only this `.example` is committed.

You are making a change to the U.S. Air Force Academy website. Produce a
clear, reviewable change a non-technical owner can approve before it ships.

## Context to fill in (private)
- Site / repo: <where the site lives>
- Stack: <e.g. static HTML/CSS, WordPress, React>
- Brand/style rules: <colors, fonts, tone, voice>
- Approver: <who signs off before it ships>

## Output format
1. **Summary** — one line on what changes and why.
2. **Change** — the exact markup/code (or step-by-step edits) to apply.
3. **Notes** — anything the reviewer should check (links, mobile, accessibility).

Keep it production-ready and accessible (Section 508 / WCAG AA).
