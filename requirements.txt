# Neo orchestrator core is stdlib-only.

# Reviewer web app (reviewer/) + Jira/Claude integrations:
fastapi>=0.115
uvicorn>=0.30
python-multipart>=0.0.9   # FastAPI form parsing (Request Changes / New Request)
requests>=2.31

# Durable storage backend (used only when DATABASE_URL is set):
psycopg[binary]>=3.1

# Payments (used only when NEO_STRIPE_* keys are set; demo mode otherwise):
stripe>=9

# Photo uploads (About photo + wall): resize + HEIC/iCloud conversion.
pillow>=10
pillow-heif>=0.16

# Encryption-at-rest for sensitive modules (used only when NEO_DATA_KEY is set).
cryptography>=42

# Transactional email (welcome email) via Resend's HTTPS API — Render's free
# tier blocks outbound SMTP, so we don't use smtplib.
resend
